PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/pbl
JWT_SECRET=replace-me-with-a-long-random-string
JWT_EXPIRES_IN=7d

# CORS: comma-separated list of allowed frontend origins (no trailing slash).
# Example: http://localhost:5173,https://pbl.example.com
ALLOWED_ORIGINS=

# HTTP logging
# In production, HTTP request logs are disabled by default.
# Set HTTP_LOG=1 to enable (uses morgan 'combined').
HTTP_LOG=

# Rate limiting (defaults are safe for small deployments)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=300

# Stricter rate limiting for /api/auth endpoints (admin/teacher login + Google login)
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX=20

# Role-based secret codes (do not expose in frontend)
# Generate strong, non-guessable secrets per environment and store securely
# (secrets manager / CI env vars). Do not commit real secrets.
TEACHER_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET
ADMIN_SECRET=REPLACE_WITH_DIFFERENT_STRONG_RANDOM_SECRET

# Google OAuth 2.0 (students only)
# Use your Google OAuth Client ID (Web) here. This is used on the server to verify ID tokens.
GOOGLE_CLIENT_ID=

# Used by seed script (optional)
SEED_ADMIN_EMAIL=admin@pbl.local

# Optional: seed a teacher user for testing Teacher Panel
SEED_TEACHER_EMAIL=teacher@pbl.local
SEED_TEACHER_NAME=Teacher

# Google Sheets sync (optional)
# Create a Google Cloud service account with Sheets API enabled.
# Share the sheet with GOOGLE_SERVICE_ACCOUNT_EMAIL.
GOOGLE_SHEETS_ID=
GOOGLE_SHEETS_TAB=Teams

# Option A (recommended): point to a downloaded service-account JSON file
GOOGLE_SERVICE_ACCOUNT_JSON_PATH=

# Option B: set credentials directly via env
GOOGLE_SERVICE_ACCOUNT_EMAIL=
# If you paste the private key here, replace newlines with \n
GOOGLE_PRIVATE_KEY=
