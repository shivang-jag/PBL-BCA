# Render deployment env (DO NOT COMMIT real secrets)
# Copy these into Render → Service → Environment.

NODE_ENV=production

# Render sets PORT automatically; keep this only if you self-host elsewhere.
# PORT=5000

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-host>/<db-name>?retryWrites=true&w=majority

# JWT
JWT_SECRET=<generate-a-long-random-secret>
JWT_EXPIRES_IN=7d

# Secret codes for teacher/admin login (do NOT expose in frontend)
TEACHER_SECRET=<generate-a-strong-random-secret>
ADMIN_SECRET=<generate-a-different-strong-random-secret>

# CORS allowlist (comma-separated, no trailing slash)
# Set this to your Vercel Production domain after frontend deploy.
# Example: https://pbl-bca.vercel.app
ALLOWED_ORIGINS=https://<your-vercel-domain>

# Optional: enable HTTP request logs in production
# HTTP_LOG=1

# Optional: rate limiting tuning (defaults already exist in code)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX=300
# AUTH_RATE_LIMIT_WINDOW_MS=900000
# AUTH_RATE_LIMIT_MAX=20

# Google OAuth: server-side verification of Google ID tokens
# Required for real Student Google login.
GOOGLE_CLIENT_ID=<your-google-oauth-web-client-id>

# Google Sheets mentor sync (only if you use it)
# GOOGLE_SHEETS_ID=
# GOOGLE_SHEETS_TAB=Teams
# GOOGLE_SERVICE_ACCOUNT_JSON_PATH=
# GOOGLE_SERVICE_ACCOUNT_EMAIL=
# GOOGLE_PRIVATE_KEY=
